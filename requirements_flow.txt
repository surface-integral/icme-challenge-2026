# requirements_flow.txt — ATTM-Mamba v2 (Flow Matching + DCAE)

# Core
torch>=2.1.0
torchaudio>=2.1.0

# Mamba CUDA kernels (install after torch)
mamba-ssm
causal-conv1d

# Text encoder (auxiliary — T5)
transformers>=4.40.0
sentencepiece>=0.1.99

# DCAE — two options (use whichever is available):
# Option A: ACE-Step package (full MusicDCAE pipeline with vocoder)
pip install git+https://github.com/ace-step/ACE-Step.git
# Option B: diffusers AutoencoderDC (DCAE without vocoder — needs HiFiGAN separately)
diffusers>=0.27.0
accelerate>=0.27.0

# Audio I/O
soundfile>=0.12.1
librosa>=0.10.0

# Utilities
numpy<2.0
tqdm>=4.66.0
omegaconf>=2.3.0
einops>=0.7.0
wandb>=0.16.0

# NTU preprocessing (step 2 only)
melband-roformer-infer
pydub>=0.25.1

# Captioning (step 4 only — needs ~20GB VRAM)
# Qwen2-Audio is loaded via transformers; no extra install needed.

# Evaluation
msclap
laion-clap
